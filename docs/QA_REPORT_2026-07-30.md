# QA Report - 2026-07-30

## Scope

- Revision tested: `af3f2b7`
- Environment: production VPS, Windows/XAMPP, PHP 8.2
- Public API: `https://apiwallet.bsolutions.dev/api/v1/`
- Report contains no credentials, OAuth tokens, email addresses, or email bodies.

## Automated Backend Tests

Command:

```powershell
composer test
```

Result:

- 100 tests passed.
- 400 assertions passed.
- Duration: 8.70 seconds.
- No failed or skipped backend tests.

Covered areas include authentication, Sanctum ownership, accounts, transactions,
cloud synchronization, OAuth state and token handling, Gmail/Microsoft mailbox sync,
email body normalization, exchange-rate conversion, duplicate candidates, refetch,
diagnostics, and financial email extraction.

## Formatting Checks

Commands:

```powershell
vendor\bin\pint --test
git diff --check
```

Result:

- Pint passed.
- Git whitespace validation passed.
- Worktree was clean before this report was created.

## Database

Command:

```powershell
php artisan migrate:status
```

Result:

- All versioned migrations are applied.
- Latest migration: `2026_07_30_180000_add_debt_id_to_transactions_table`.
- No pending migrations.

## Production Health

Request:

```powershell
curl.exe https://apiwallet.bsolutions.dev/api/v1/health
```

Result:

- HTTP status: `200`.
- Two Laravel queue workers were active during verification.
- Queue snapshot: 0 queued jobs, 0 entries in `failed_jobs`.
- Historical failed sync runs: 118, all intentionally marked
  `email_sync_superseded` when a duplicate backlog was consolidated.

## Email Classification Metrics

Command:

```powershell
php artisan email:diagnose --show=0
```

Result:

- Stored emails analyzed: 465.
- Recognized as financial movements: 79 (17%).
- Discarded as non-movements: 386 (83%).
- Recognized movements without merchant: 21 (27%).
- Recognized movements without a specific category: 30 (38%).
- Recognized movements without card digits: 19 (24%).
- Median available body text: 255 characters.
- Maximum available body text: 4000 characters.
- Empty body text records: 0.

These metrics describe existing stored messages. New messages use full MIME/HTML body
normalization; older records only improve when `email:refetch-bodies` retrieves a
longer provider body.

## Android Verification Gap

Android Gradle tests and APK assembly were not run on this VPS because Java is not
installed and `JAVA_HOME` is unavailable. Android verification must run on a machine
with the project's required JDK and Android SDK. This does not affect the backend test
results above.
